package io.olo.definition.planner;

/**
 * Kind of planner catalog entry (workflow, agent, tool, or node-level capability).
 */
public enum CatalogKind {
    WORKFLOW,
    AGENT,
    TOOL,
    NODE
}
